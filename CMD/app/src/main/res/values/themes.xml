<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CMD" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="MainActivityTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- MainActivity에서 사용할 네비게이션 바 색상 설정 -->
        <item name="android:navigationBarColor">@color/green2</item>
        <item name="colorPrimary">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="colorPrimary">#8BC34A</item>
    </style>

    <style name="Theme.CMD" parent="Base.Theme.CMD" />
    
</resources>